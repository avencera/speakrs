# WavLM bridge fixture

This empty-recording fixture checks the strict v1 manifest, canonical digest,
fractional receptive-field origin, aggregate-grid extent policy, and publication marker.  Tests create
bounded tensor shards in temporary directories for value and corruption cases.
